Default procedural background used by bg_layer.v.

The current FPGA implementation draws this background in logic instead of reading a bitmap.
Add future background folders under backgrounds/<name>/ and extend bg_layer.v/background_control.v accordingly.
